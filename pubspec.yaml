name: qs_common
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2

  shared_preferences: ^0.5.6+1
  event_bus: ^1.1.0 # 事件总线
  dio: ^3.0.7 #网络请求库
  dio_http_cache: ^0.2.5 #dio 缓存库
  device_info: ^0.4.0+3
  connectivity: ^0.4.6 #网络状态监听工具
  fluttertoast: ^3.1.3
  encrypt: ^3.2.0
  pointycastle: ^1.0.2
  provider: ^3.0.0+1 #状态管理

  permission_handler: ^4.3.0 #权限处理
  path_provider: ^1.6.1 #获取文件路径
  image_pickers: ^1.0.7+1 #选择图片
  url_launcher: ^5.4.2
  get_it: ^1.0.3+2 # 服务

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #  - images/a_dot_burr.jpeg
  #  - images/a_dot_ham.jpeg
